# Proposal — Rediseño Editorial Total

<!-- Scaffold inicial (sdd-init). Fase actual: explore (pre-proposal).
     El contenido de propuesta se redacta en la fase proposal; aquí solo se
     fija el charter del change. NO proponer diseño todavía. -->

## Change metadata

- **Change:** `redesign-editorial-total`
- **Branch:** `redesign/editorial-total` (creada desde `main` 2af28bb)
- **Modo:** execution auto ("dale") · delivery ask-on-risk · review budget 400 líneas

## Charter (fijo, no negociable)

- **Qué:** rediseño visual total del storefront editorial Flores Crimson — home,
  PDP, catálogo y tokens CSS (`src/app/globals.css`), bajo la dirección
  "The Crimson Atelier" de DESIGN.md.
- **Qué NO:** backend Convex (`convex/`), admin (`src/app/admin/*`),
  carrito/checkout (Zustand `src/lib/store.ts`, `CartDrawer`, `WhatsAppButton`),
  CMS, API routes, `next.config.ts`, `database/`.
- **Fuente de verdad:** `DESIGN.md` + `PRODUCT.md`.

## Estado del ciclo

- [ ] explore — pendiente (solo lectura: storefront actual vs. DESIGN.md)
- [ ] proposal — pendiente (redactar aquí la propuesta de rediseño)
- [ ] spec — pendiente
- [ ] design — pendiente
- [ ] tasks — pendiente
- [ ] apply — pendiente
- [ ] verify — pendiente
- [ ] sync — pendiente
- [ ] archive — pendiente

## Preguntas abiertas (las responde explore antes de proposal)

> (vacío — backfill en fase explore)