# Legacy — Archivos históricos no vigentes

> **Fuente de verdad actual:** `DESIGN.md` + `PRODUCT.md` en la raíz del proyecto.
> Todo archivo bajo `docs/legacy/` es **solo histórico** — no usar para implementar, diseñar ni decidir.

## PROJECT_CONTEXT.md — archivado 2026-05-13

**Origen:** `PROJECT_CONTEXT.md` (raíz) movido a `docs/legacy/PROJECT_CONTEXT.md` en commit `chore: archivar PROJECT_CONTEXT legacy`.

**Por qué se archivó:**
- Contradice la identidad actual **Flores Crimson** (`DESIGN.md`).
- Describe "Aria Liquidación" amarilla `#FFD700`, Montserrat, `border-radius: 20px` y backend Node/Postgres `178.238.227.97:3005` — todo reemplazado.
- Stack real es **Next.js 16 + React 19 + Tailwind 4 + Convex + Zustand + Cloudinary** con paleta `Flores Crimson #9B1C1C`, sharp `radius 0`, mujer-first y `next/font`.
- Instrucciones para "Jules" y próximos pasos (videos, WhatsApp genérico) ya resueltos o superados por `lib/whatsapp.ts`, `lib/site.ts` y `CartDrawer` stepper Review→Data→WhatsApp.

**Regla:** No reintroducir valores de este archivo. Si encontrás una referencia a él, actualizala a `DESIGN.md`/`PRODUCT.md`.
